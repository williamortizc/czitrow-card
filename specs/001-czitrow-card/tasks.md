# Tasks: Czitrow Card

**Input**: Design documents from `/specs/001-czitrow-card/`
**Prerequisites**: plan.md (required), spec.md (required), research.md, data-model.md, quickstart.md

**Tests**: No automated tests — this is a static site. Verification is visual/manual per constitution.

**Organization**: Tasks grouped by user story for independent implementation and testing.

## Format: `[ID] [P?] [Story] Description`

- **[P]**: Can run in parallel (different files, no dependencies)
- **[Story]**: Which user story this task belongs to (US1, US2, US3)
- Include exact file paths in descriptions

## Phase 1: Setup (Shared Infrastructure)

**Purpose**: Create the project files that everything else depends on.

- [x] T001 Create `CNAME` file with content `czitrow.xyz` at repository root
- [x] T002 Create empty `styles.css` at repository root with CSS reset and base dark theme variables
- [x] T003 Create `index.html` at repository root with minimal HTML5 boilerplate: `<!DOCTYPE html>`, `<html lang="es">`, `<head>` with charset, viewport, title, and link to `styles.css`, empty `<body>`

**Checkpoint**: Three files exist at repo root (`index.html`, `styles.css`, `CNAME`). Page renders blank with dark background.

---

## Phase 2: Foundational (Blocking Prerequisites)

**Purpose**: Core HTML structure and CSS foundations that ALL user stories depend on.

**⚠️ CRITICAL**: No user story work can begin until this phase is complete.

- [x] T004 Add semantic HTML structure to `index.html`: `<main>` container with flexbox centering, `<header>` for name/hero, `<nav>` for links, `<section>` for description, `<footer>` for domain and year
- [x] T005 Add CSS custom properties (variables) to `styles.css` for the dark theme: `--bg: #0a0a0a`, `--text: #e0e0e0`, `--accent: #00d4aa`, `--muted: #666666`, plus base typography (system font stack, sizes, line-height)
- [x] T006 Add responsive layout CSS to `styles.css`: body flexbox centering with `min-height: 100vh`, container `max-width: 600px` with padding, and media queries for 320px+ / 768px+ / 1440px+

**Checkpoint**: Page renders with dark background, centered container, and system fonts. Structure is ready for content.

---

## Phase 3: User Story 1 - Visitor sees the complete page (Priority: P1) 🎯 MVP

**Goal**: A visitor navigates to `czitrow.xyz` and sees the owner's name, tagline, 3 links, description, and footer on a single cleanly designed dark page.

**Independent Test**: Open `index.html` in a browser. All content is visible, links open in new tabs, layout is responsive, page loads with only 2 HTTP requests.

- [x] T007 [US1] Add the name "William Ortiz" as an `<h1>` element inside the `<header>` in `index.html`, styled large, bold, light color (`var(--text)`)
- [x] T008 [US1] Add the tagline as a `<p>` element below the name in `index.html`, styled smaller, muted color (`var(--muted)`). Placeholder text: "Notas, ideas y cosas útiles." (owner to replace)
- [x] T009 [US1] Add 3 links as an unordered list `<ul>` inside `<nav>` in `index.html`, each `<a>` with `target="_blank"` and `rel="noopener noreferrer"`. Link labels in Spanish. Placeholder links:
  1. "GitHub" → `https://github.com/williamortizc`
  2. "Notas" → `#placeholder`
  3. "Contacto" → `mailto:williamcr1588@gmail.com`
- [x] T010 [US1] Add link styling to `styles.css`: remove list bullets, style links with `var(--accent)` color, hover effect, vertical spacing between links
- [x] T011 [US1] Add a description `<section>` in `index.html` with a `<p>` containing 1-3 sentences in Spanish. Placeholder: "Este es mi rincón mínimo en internet. Aquí comparto procedimientos técnicos, soluciones y notas que me son útiles, y quizá a alguien más."
- [x] T012 [US1] Add the `<footer>` in `index.html` with "czitrow.xyz · 2026", styled small text, `var(--muted)` color, centered
- [x] T013 [US1] Verify: open `index.html` in browser and confirm all content is visible — name, tagline, 3 links, description, footer. Check responsive at 320px, 768px, 1440px widths. Check that links open in new tabs.

**Checkpoint**: At this point, the page is fully functional as a personal card. This is the MVP. All 13 functional requirements (FR-001 through FR-013) should be met.

---

## Phase 4: User Story 2 - Owner edits content easily (Priority: P2)

**Goal**: The site owner can open `index.html` in any plain text editor, find content quickly, change text, save, and see changes reflected.

**Independent Test**: Open `index.html` in a text editor, search for "William Ortiz" or the tagline text, change it, save, refresh browser. Change is visible.

- [x] T014 [US2] Add HTML comments to `index.html` marking each content section clearly: `<!-- NAME -->`, `<!-- TAGLINE -->`, `<!-- LINKS -->`, `<!-- DESCRIPTION -->`, `<!-- FOOTER -->`. This makes the file self-documenting for easy editing.
- [x] T015 [US2] Review `index.html` structure and ensure all editable content is inline (not generated by scripts, not hidden in external files, not minified). Content must be plain text in the HTML that anyone can find and change.
- [x] T016 [US2] Verify: open `index.html` in a plain text editor, search for "William Ortiz", change it to something else, save, refresh browser. Confirm the change is visible. Revert the change.

**Checkpoint**: The file is trivially editable. Any content change can be made by finding and replacing plain text.

---

## Phase 5: User Story 3 - Site loads fast with no dependencies (Priority: P3)

**Goal**: The page loads with only 2 HTTP requests (HTML + CSS), zero external dependencies, and renders in under 2 seconds.

**Independent Test**: Open browser DevTools Network tab, load the page. Confirm only `index.html` and `styles.css` are requested. No external fonts, scripts, or CDN resources.

- [x] T017 [US3] Audit `index.html` for external resource references: check that no `<link>` points to CDN fonts, no `<script>` tags exist, no `<img>` with external URLs. All styling comes from `styles.css` only.
- [x] T018 [US3] Audit `styles.css` for external resource references: check that no `@import` or `url()` points to external resources. All font references use system font stack only.
- [x] T019 [US3] Verify: open DevTools Network tab, load `index.html`, confirm exactly 2 requests (`index.html` + `styles.css`), zero external dependencies, page renders under 2 seconds.

**Checkpoint**: The page loads with minimal requests. Performance and dependency constraints are verified.

---

## Phase 6: Polish & Cross-Cutting Concerns

**Purpose**: Final quality improvements that affect the entire site.

- [x] T020 Verify all functional requirements (FR-001 through FR-013) are met by testing each one manually
- [x] T021 Validate HTML with W3C validator or equivalent — ensure `index.html` has no errors
- [x] T022 Test responsive design at 320px (mobile), 768px (tablet), and 1440px (desktop) viewports — confirm no overflow or broken layout
- [x] T023 Add `<meta name="description">` tag to `index.html` `<head>` with a brief site description in Spanish (SEO future-proofing, does not violate constitution)
- [x] T024 Commit all changes with descriptive git messages, verify git history is clean

---

## Dependencies & Execution Order

### Phase Dependencies

- **Setup (Phase 1)**: No dependencies — can start immediately
- **Foundational (Phase 2)**: Depends on Phase 1 completion — BLOCKS all user stories
- **User Story 1 (Phase 3)**: Depends on Phase 2 completion — MVP
- **User Story 2 (Phase 4)**: Depends on Phase 3 completion (needs content in place first)
- **User Story 3 (Phase 5)**: Can start after Phase 3 completion (needs page built to audit)
- **Polish (Phase 6)**: Depends on all user stories being complete

### User Story Dependencies

- **User Story 1 (P1)**: Depends on Setup + Foundation only — MVP deliverable
- **User Story 2 (P2)**: Depends on US1 (needs content to verify editability)
- **User Story 3 (P3)**: Depends on US1 (needs built page to audit performance)

### Parallel Opportunities

- T001, T002, T003 can all run in parallel (different files)
- T007, T008, T009, T011 can run in parallel (different HTML sections, same file — careful) 
- T010 and T012 can run in parallel with content tasks (different concerns)
- T017 and T018 can run in parallel (auditing different files)

---

## Parallel Example: Phase 3 (User Story 1)

The content tasks (T007, T008, T009, T011) each add content to different sections of `index.html`. While they touch the same file, they are independent sections and can be added in any order:

```bash
# Add all content sections (order doesn't matter):
Task T007: "Add name <h1> in header"
Task T008: "Add tagline <p> below name"
Task T009: "Add 3 links in <nav>"
Task T011: "Add description <section>"

# Then style them together:
Task T010: "Style links in styles.css"
Task T012: "Add footer"
```

---

## Implementation Strategy

### MVP First (User Story 1 Only)

1. Complete Phase 1: Setup → 3 files created
2. Complete Phase 2: Foundation → dark theme, layout, typography ready
3. Complete Phase 3: User Story 1 → name, tagline, links, description, footer all working
4. **STOP and VALIDATE**: Open `index.html` in browser. Verify everything works.
5. The site is usable at this point. Deploy if desired.

### Incremental Delivery

1. Setup + Foundation → files exist, dark theme works
2. Add User Story 1 → **MVP!** Page is fully functional as a personal card
3. Add User Story 2 → Easy to edit, self-documenting HTML
4. Add User Story 3 → Verified performance and zero dependencies
5. Polish → Validated, clean, ready for production

---

## Notes

- This is a static site with 2-3 files. No build tools, no testing framework, no CI/CD.
- Verification is visual and manual: open in browser, check layout, check DevTools.
- All content is in Spanish per FR-012.
- Dark theme per FR-010 with specific color palette from research.md.
- Links open in new tabs per FR-011 with `rel="noopener noreferrer"` for security.
- The name "William Ortiz" is fixed per FR-013.
- The tagline, link URLs, and description text are placeholders that the owner can change at any time by editing `index.html` directly per FR-009.